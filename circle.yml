deployment:
  production:
    branch: master
    commands:
      - rsync -az ../bookdweb/ circleci@buzapp.me:/home/circleci/bookdweb
  staging:
    branch: development
    commands:
      - rsync -az ../bookdweb/ circleci@dev.buzapp.me:/home/circleci/bookdweb

machine:
  node:
    version: 6.5.0

dependencies:
  cache_directories:
    - "bower_components"

test:
  override:
    - echo "test"